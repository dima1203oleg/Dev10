# Server & AI Configurations
GEMINI_API_KEY=
TEMPORAL_ADDRESS=127.0.0.1:7233
TEMPORAL_NAMESPACE=default
CLAMAV_HOST=127.0.0.1
CLAMAV_PORT=3310
DOCLING_URL=http://127.0.0.1:5001
DOCLING_API_KEY=
STORAGE_DRIVER=s3
S3_BUCKET=tenderai-documents
S3_ENDPOINT=http://127.0.0.1:8333
S3_REGION=us-east-1
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
NODE_ENV=production
PORT=3000

# PostgreSQL Database Credentials
# Hosted PostgreSQL option (Neon/Supabase). When set, this takes precedence over SQL_*.
# Keep credentials out of Git. Use sslmode=require for hosted providers.
DATABASE_URL=
DATABASE_ADMIN_URL=

SQL_HOST=
SQL_USER=
SQL_PASSWORD=
SQL_DB_NAME=
SQL_ADMIN_USER=
SQL_ADMIN_PASSWORD=

# Firebase Auth Configuration
FIREBASE_PROJECT_ID=
LOCAL_DEV_AUTH=false
LOCAL_DEV_FIREBASE_UID=
LOCAL_DEV_EMAIL=
ALLOW_DEV_AUTH=
# Client-side dev auth is separately gated and must stay false in production.
VITE_ALLOW_DEV_AUTH=false
